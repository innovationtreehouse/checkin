// Just testing compilation to make sure type-checks pass for the fix
import { PrismaClient } from '@prisma/client';
import { POST } from './src/app/api/events/[id]/attendance/route';
console.log("Compilation checks out");
