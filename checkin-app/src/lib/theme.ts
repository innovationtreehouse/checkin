import { createTheme } from '@mantine/core';

/**
 * Stock Mantine theme — intentionally empty to match the sibling `inventory` apps,
 * which all use `createTheme({})` (default blue primary, system font stack, light/auto
 * color scheme). Keep customizations here if checkin ever needs to diverge.
 */
export const theme = createTheme({});
